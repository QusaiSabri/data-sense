namespace data_sense.Services
{
    public class DatabaseSelectionService
    {
        //private readonly IDataService _dataService;

        //public DatabaseSelectionService(IDataService dataService)
        //{
        //    _dataService = dataService;
        //}

        //public async Task<IDatabase> GetSelectedDatabase(string databaseType)
        //{
        //    // Get the database using the factory in the _dataService
        //    return await _dataService.GetDatabase(databaseType);
        //}
    }
}
